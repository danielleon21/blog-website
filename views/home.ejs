<%- include('partials/header') -%>

<h1>Home</h1>

<p><%= startingContent %></p>

<% if(posts.length > 0) { %> <% posts.forEach(post => { %>
<h3><%= post.title %></h3>
<p><%= post.content.substring(0, 100) + "..." %> <a href=<%= "/post/" + post.title %> class="text-decoration-none">Read More </a> </p>

<% }) %> <% } %> <%- include('partials/footer') -%>